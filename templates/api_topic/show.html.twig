{% extends 'base.html.twig' %}

{% block title %}Topic{% endblock %}

{% block body %}
    <h1>Topic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ topic.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ topic.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('api_topic_get_all') }}">back to list</a>

    <a href="{{ path('api_topic_edit', {'id': topic.id}) }}">edit</a>

    {{ include('api_topic/_delete_form.html.twig') }}
{% endblock %}
